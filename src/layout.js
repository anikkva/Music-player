// Where the radio sits in the cabin. Shared so the centre stack around it is
// built in the same rotated frame — otherwise the console's flat front clips
// the turned faceplate.

export const RADIO_POSITION = [0.34, -0.31, -0.735];
export const RADIO_YAW = -0.386;   // turned toward the driver
export const RADIO_PITCH = -0.20;  // tilted up a little
